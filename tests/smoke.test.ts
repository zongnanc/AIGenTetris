import { describe, it, expect } from "vitest";

// Placeholder so the test runner has something to run at M0.
// Replaced by real logic tests starting at M2.
describe("smoke", () => {
  it("runs the test suite", () => {
    expect(1 + 1).toBe(2);
  });
});
